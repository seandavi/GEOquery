% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sra.R
\name{geoToSRA}
\alias{geoToSRA}
\title{Resolve a GEO accession to its SRA accessions}
\usage{
geoToSRA(geo)
}
\arguments{
\item{geo}{character(1), a GEO accession (\code{GSE...} or \code{GSM...}).}
}
\value{
A \code{data.frame} with one row per SRA run and columns
\code{geo_accession}, \code{srp}, \code{srx}, \code{srr}, \code{srs}. Returns a zero-row data.frame
(same columns) when the accession has no linked SRA records.
}
\description{
Maps a GEO Series (\code{GSE}) or Sample (\code{GSM}) to the underlying Sequence Read
Archive (SRA) accessions — study (\code{SRP}), experiment (\code{SRX}), run (\code{SRR}),
and sample (\code{SRS}) — by cross-referencing NCBI Entrez (\code{gds} -> \code{sra}). This
is the accession-linking bridge to raw sequencing data; it downloads no
sequence files.
}
\details{
Uses the NCBI Entrez API through \pkg{rentrez}. An \code{ENTREZ_KEY} raises the
rate limit; see \link{searchGEO} for how to set one.
}
\examples{
\dontrun{
geoToSRA("GSE164073")
}

}
\seealso{
\link{searchGEO}
}
